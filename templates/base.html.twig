<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>

        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
       
        <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">

        <link rel="stylesheet" href="/css/base.css">
        
        {% block stylesheets %}{% endblock %}
    </head>
    <body class="bodybackground">

        <nav class="navbar navbar-expand-lg navbar-dark bg-primary" >
            <img class="logo" src="/images/Unicornis.png" alt="">
            <a class="navbar-brand" href="#">Unicornis</a>
            <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="navbar-collapse collapse" id="navbarColor01" style="">
                <ul class="navbar-nav mr-auto">
                
                    <li class="nav-item ">
                        <a class="nav-link" href="{{path('homepage')}}">News <span class="sr-only">(current)</span></a>
                    </li>

                    <li class="nav-item ">
                       <div class="dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Tests de Jeux</a> <!-- lien -->
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="#">Jeu d'Equitation</a>
                            <a class="dropdown-item" href="#">Autre</a>
                        </div>
                        </div>
                    </li>

                    <li class="nav-item ">
                       <div class="dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Art</a> <!-- lien -->
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{path('coloriage')}}">Coloriages<span class="sr-only">(current)</span></a>
                            <a class="dropdown-item" href="{{path('wall_of_fame')}}"> Wall of Fame <span class="sr-only">(current)</span></a>
                        </div>
                        </div>
                    </li>

                    <li class="nav-item ">
                       <div class="dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Vos Questions</a> <!-- lien -->
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{path('questions')}}">Questions Fréquentes</a>
                            <a class="dropdown-item" href="{{path('materiel')}}">Matériel</a>
                            <a class="dropdown-item" href="{{path('contact')}}">Contact</a>
                        </div>
                        </div>
                    </li>

                    <li class="nav-item" style="position:absolute; right:0em">
                        {% if app.user %}
                        <a class="nav-link" href="{{path('app_logout')}}">Déconnexion</a>
                        {% else %}
                        <a class="nav-link" href="{{path('app_login')}}">Connexion</a>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </nav>
        
        <div class="bodybackground">
            {% block body %}{% endblock %}
        </div>

        <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    </body>
</html>
